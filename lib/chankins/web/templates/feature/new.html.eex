<h2>New Feature</h2>

<%= render "form.html", changeset: @changeset, versions: @versions,
                        action: feature_path(@conn, :create) %>

<span><%= link "Features", to: feature_path(@conn, :index) %></span>
