<h2>Show Project</h2>

<h3><%= @project.name %></h3>
<h4>Releases:</h4>
<ul>
  <%= for release <- @project.releases do %>
    <li> <%= link release.name, to: release_path(@conn, :show, release) %> </li>
  <% end %>
</ul>

<span><%= link "Edit", to: project_path(@conn, :edit, @project) %></span>
<span><%= link "Back", to: project_path(@conn, :index) %></span>
