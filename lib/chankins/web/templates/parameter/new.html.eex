<h2>New Parameter</h2>

<%= render "form.html", changeset: @changeset,
                        action: parameter_path(@conn, :create) %>

<span><%= link "Back", to: parameter_path(@conn, :index) %></span>
