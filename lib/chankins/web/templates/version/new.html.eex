<h2>New Version</h2>

<%= render "form.html", changeset: @changeset, releases: @releases,
                        action: version_path(@conn, :create) %>

<span><%= link "Versions", to: version_path(@conn, :index) %></span>
